import React from 'react';
import { Button, View } from 'react-native';

const AuthButtons = () => {
  return (
    <View>
      <Button title="로그인" />
      <Button title="로그아웃" />
    </View>
  );
};

export default AuthButtons;
